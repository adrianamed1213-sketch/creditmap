import { ArrowRight, LockKeyhole, ShieldCheck } from "lucide-react";
import Link from "next/link";

export function DemoAuthPage({ mode }: { mode: "login" | "signup" }) {
  const login = mode === "login";
  return <section className="page-shell grid min-h-[65vh] place-items-center py-14"><div className="w-full max-w-lg rounded-3xl border border-[var(--line)] bg-white p-6 text-center shadow-[0_20px_50px_rgba(21,55,65,0.08)] sm:p-9"><div className="mx-auto grid size-12 place-items-center rounded-2xl bg-[var(--mint-50)] text-[var(--brand-700)]"><LockKeyhole aria-hidden="true" className="size-6" /></div><p className="mt-6 text-xs font-bold tracking-[0.1em] text-[var(--brand-700)] uppercase">{login ? "Sign in" : "Create account"}</p><h1 className="mt-2 text-3xl font-extrabold tracking-[-0.045em] text-[var(--brand-950)]">Cloud accounts are optional for this demo.</h1><p className="mt-4 leading-7 text-[var(--text-muted)]">The competition version saves one plan locally so judges can use the complete flow without email or network risk. Supabase Auth and ownership policies are included in the production schema but require project credentials.</p><Link className="primary-button mt-7 w-full" href="/dashboard">Continue to local demo<ArrowRight aria-hidden="true" className="size-4" /></Link><div className="mt-5 flex items-center justify-center gap-2 text-xs font-semibold text-[var(--text-muted)]"><ShieldCheck aria-hidden="true" className="size-4 text-[var(--brand-600)]" />No password or personal information collected</div><p className="mt-6 text-sm text-[var(--text-muted)]">{login ? "Need the first-time flow?" : "Already explored the app?"} <Link className="font-bold text-[var(--brand-700)] hover:underline" href={login ? "/start" : "/login"}>{login ? "Build a new map" : "View sign-in information"}</Link></p></div></section>;
}
